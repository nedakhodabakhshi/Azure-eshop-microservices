
apiVersion: v1
kind: Service
metadata:
  name: ordering-api
  labels: { app: ordering-api }
spec:
  selector: { app: ordering-api }
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP
